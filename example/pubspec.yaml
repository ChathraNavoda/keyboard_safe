name: example
description: "A demo app for the keyboard_safe package."
publish_to: 'none' # This prevents publishing the example app to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  keyboard_safe:
    path: ../


  # Optional icons package
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
